/* register_types.h */
#include "modules/register_module_types.h"

void register_qurobullet_types(ModuleInitializationLevel p_level);
void unregister_qurobullet_types(ModuleInitializationLevel p_level);
/* yes, the word in the middle must be the same as the module folder name */
